# Examples

Quick demo examples and usage notes:

- Run the CLI demo (text input simulates voice):

    make demo

- Sample commands:
  - `wifi toggle` — toggles wifi and then checks network status (multi-step)
  - `shutdown system` — requires confirmation when confidence is low

For more advanced examples (LLM prompts, training data export), see `docs/`.
